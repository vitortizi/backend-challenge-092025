# from pathlib import Path
# import sys

# ROOT = Path(__file__).resolve().parents[1]
# if str(ROOT) not in sys.path:
#     # Ensure repository root is importable even if pytest rootdir is tests/.
#     sys.path.insert(0, str(ROOT))

####################################################################################
# Descomentar o código acima se não quiser passar (python -m) para rodar o pytest
####################################################################################